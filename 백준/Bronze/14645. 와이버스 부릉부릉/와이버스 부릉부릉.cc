#include<iostream>
#include<algorithm>
#include<vector>
#include<string>
#include<stack>
#include<string.h>
using namespace std;
int n,m,sum,k;

int main() {
	scanf("%d %d", &n,&k);
	for (int i = 0; i < n; i++) {
		scanf("%d %d", &m, &sum);
	}
	printf("비와이");
	return 0;
}