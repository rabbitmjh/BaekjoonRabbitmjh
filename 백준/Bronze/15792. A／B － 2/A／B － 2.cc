#include <iostream>
#include <algorithm>
#include <utility>
#include <functional>
#include <vector>
#include <queue>
#include <string>
#include <cstring>
#include <cmath>
#include <limits>
using namespace std;
typedef long long ll;
typedef pair <int, int> p;
typedef pair <int, p> pp;
//백준1931 회의실배정

int arr[100005][2];
ll ans, sum;

int main(){
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	long double a, b;
	cin >> a >> b;
	cout << a / b << "\n";
	
    return 0;
}
