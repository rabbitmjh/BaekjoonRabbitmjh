#include <iostream>
#include <algorithm>
#include <string.h>
#include <string>
#include <vector>
#include <stack>
using namespace std;
string a;
int s[26];
int n;

int main() {
	cin >> a;
	printf("%d", a.size());
	return 0;
}